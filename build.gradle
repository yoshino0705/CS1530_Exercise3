apply plugin: 'java'
apply plugin: 'application'

mainClassName = "chen.Excercise3"

repositories {
   mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

task sequencehelp << {
  println "Integer Sequence Project"
  println " 'Lazy' following an integer of choice "
  println " or "
  println " 'Triangle' following an integer of choice "
  println "To print the result of either the Lazy Caterer number or the Triangle number"
}

run {
    try {
        systemProperties System.getProperties()
	     args System.getProperty("args").split()
    } catch (Exception e) {
        // Do nothing
    }
	
}
